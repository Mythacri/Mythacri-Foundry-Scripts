<div class="content">
  <div class="header">
    <img src="{{target.img}}" data-tooltip="{{targetHint}}" data-tooltip-class="{{targetCss}}">
    {{target.name}}
    <a data-tooltip="MYTHACRI.CRAFTING.HANDLER.hint" data-tooltip-direction="UP">
      <i class="fa-solid fa-question-circle"></i>
    </a>
  </div>
  <div class="components">
    {{#each context}}
    <div class="column">
      <div class="component-header">
        <img src="{{icon}}" data-identifier="{{identifier}}">
        <span class="label">{{label}}</span>
        <span class="counter">({{ifThen assigned assigned.system.quantity 0}}/{{quantity}})</span>
      </div>
      <div class="resources">
        {{#each resources}}
        <div class="component">
          <a data-identifier="{{../identifier}}" data-item-id="{{resource.id}}" data-tooltip="{{tooltipHint}}" data-tooltip-class="{{tooltipCss}}">
            <img src="{{resource.img}}" {{#if active}} class="active" {{/if}}>
          </a>
          <span class="quantity">{{localize "DND5E.Quantity"}}: {{resource.system.quantity}}</span>
        </div>
        {{/each}}
      </div>
    </div>
    {{/each}}
  </div>
  <div class="footer">
    <button type="button" class="craft" {{disabled noCreate}}>
      <i class="fa-solid fa-check"></i> {{localize "Confirm"}}
    </button>
  </div>
</div>
